<p id="notice"><%= notice %></p>

<h1>Lists#show</h1>

<h1><%= @list.name %></h1>

<div>
  <%= link_to 'Add Locations', edit_list_path(@list) %> |
  <%= link_to 'Edit', edit_list_path(@list) %> |
  <%= link_to 'Back', lists_path %>
</div>  
<br>

<%= render @list.locations %>